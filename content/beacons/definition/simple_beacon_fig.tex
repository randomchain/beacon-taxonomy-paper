\begin{figure}[htb]\pgfdeclarelayer{background}
    \pgfsetlayers{background,main}
    \centering
    \footnotesize
    \begin{tikzpicture}[auto]
        \node[block] (input) {Entropy source};
        \node[block, below=1cm of input] (computation) {Computation\\$f(I_t)$};
        \draw[arrow] (input) -- node[yshift=-1.5mm] (inp) {$I_t$} (computation);
        \node[block, below=1cm of computation] (users) {Users};
        \draw[arrow] (computation) -- node[yshift=1.5mm] (out) {$O_t$} (users);
        \begin{pgfonlayer}{background}
            \node[block] (beacon) [container, fit={([shift={(0mm,-2mm)}]inp.north) (computation) ([shift={(0mm,2mm)}]out.south)}] {};
        \end{pgfonlayer}
    \end{tikzpicture}
    \caption{Abstract randomness beacon. The green marking denotes the beacon protocol, $B$.}\label{fig:abstract_beacon}
\end{figure}
