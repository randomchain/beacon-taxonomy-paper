\section{Paper Summaries}\label{sec:papersummaries}

We will shortly summarize the papers that we analyze as part of our taxonomy.

%foreach(p in papers){
%   write(p.motivation);
%   write(p.contribution);
%   write(p.evaluation);
%   write(p.connectionToOurBeaconDefs);

%}
%yes im aware the syntax could be better, but it organizes thoughts.

%here goes the papers - need to fiddle with order to deliver message.
%Maybe we should also add NIST beacon, since we use it to compare. Also RANDAO?
%redefine command below if not in description env
\newcommand{\papertitle}[1]{\item[#1]\hfill\\}
\begin{description}
    \subimport{}{nist.tex}
    \subimport{}{randomservice.tex}
    \subimport{}{findata.tex}
    \subimport{}{mildollarcurve.tex}
    \subimport{}{onbitcoin.tex}
    \subimport{}{pibeacon.tex}
    \subimport{}{randhound.tex}
    \subimport{}{randao.tex}
    \subimport{}{scrape.tex}
    \subimport{}{randzoo.tex}
    \subimport{}{proofofdelay.tex}
\end{description}

